## Project Rules Learned

1. All forms must use controlled React components for consistent state management.
2. Every form input must have a visible label connected using `htmlFor` and `id`.
3. Validate all required fields before allowing form submission.
4. Disable the submit button until all validation requirements are satisfied.
5. Review AI-generated code for compatibility with the existing project structure before committing changes.
